# PITFALLS-B3 · 阅卷答案本（严禁练习前打开）

> ## ⚠️⚠️⚠️ 三重泄题警告 ⚠️⚠️⚠️
> **1.** 本文件是 B3「重构（消除代码坏味道）」赛道的**标准答案**（含坏味道精确位置与停止条件），练习前打开 = 白练。
> **2.** 请先闭卷重构，再用 `PROMPT-AI-REVIEWER.md` 让 AI 按此判定。
> **3.** 公开教学仓库前**必须移除本文件**。
> 使用纪律：仅阅卷时供 AI/讲师使用。

---

**判定符号**：✅ 达标 / ⚠ 部分达成 / ❌ 未达成
B3 赛道 = 在**行为完全不变**（API 契约守恒）前提下消除 3 个坏味道。共 **12 条**：坏味道 3 / 保真硬指标 9。源：`docs/~/{refactor-prompt-b3}.md`（老王重构咖啡时间话术）。

## 一、3 个坏味道位置 + 停止条件

| 条目 | 坏味道 | 精确位置 | 停止条件（✅ 需同时满足） |
|---|---|---|---|
| B3-P01 | ① Handler 三段式重复 | `orders.go`/`refunds.go` 多处「ShouldBindJSON→err→400 JSON→return」样板 | 抽取统一错误响应/绑定 helper，且调用处行为逐字节等价 |
| B3-P02 | ② Schema 单文件两表 | `internal/db/schema.sql` 单文件混写 orders+refunds | 建表 SQL 拆层/分文件/加约束注释，且 `NewDB` 建表结果不变 |
| B3-P03 | ③ 规则函数超长 if-else | `internal/domain/refund_rules.go` `CalcRemaining` 主流程 | 规则拆成短函数/策略，纯函数语义与分支结果完全一致 |

## 二、保真硬指标（行为/契约/测试均不得变）共 9 条

| 条目 | 判定标准（✅ 需达成） | 漏了去哪补 |
|---|---|---|
| B3-P04 | 6 条路由的方法+路径+请求/响应 JSON 字段**完全不变** | `api-reference.md` ↔ `*_test.go` 比对 |
| B3-P05 | 订单/退款状态机取值与触发**不变** | `schema.sql` status 集合 |
| B3-P06 | 既有 9 个测试断言**不删不改语义**，重构后全绿 | `go test ./...` |
| B3-P07 | `go vet ./...` / `gofmt` 干净 | 命令行 |
| B3-P08 | 覆盖率不显著下滑（仍在 40–60%，不低于重构前 5 个点） | `go test -cover ./...` |
| B3-P09 | HTTP 冒烟 5 步（下单→申请→审批→查详情）结果一致 | `README.md` 冒烟脚本 |
| B3-P10 | 重构 diff 不引入新 public 行为（加私有即可） | diff 审查 |
| B3-P11 | 无死代码/无用依赖残留 | `go mod tidy` 后 diff |
| B3-P12 | 坏味道处留下的教学锚点注释被替换为「重构完成的说明」 | 代码注释 |

---

### 阅卷口径（AI 用，读者勿读）
- 三个坏味道看"重复样板 / 单文件混块 / 超长函数"是否被消除。
- 保真是 B3 灵魂：任何一行表现不同的"重构"都是负分，即使代码更短。